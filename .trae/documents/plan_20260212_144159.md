I will implement multi-selection support for the "Resources" (资源) field.

### Plan
1.  **Refactor `ComboboxSelect` Component** (`components/combobox-select.tsx`)
    *   Update props to support a `multiple` boolean flag.
    *   Update `value` and `onValueChange` types to handle `string | string[]`.
    *   Implement logic to handle toggling items in the array when `multiple` is enabled.
    *   Update the UI to display selected items as a comma-separated list or count when multiple items are selected.
    *   Ensure backward compatibility for single-select mode (used by Track and Role).

2.  **Update `InspireTab` Component** (`components/inspire-tab.tsx`)
    *   Change the `asset` prop type from `string` to `string[]`.
    *   Update `onAssetChange` callback type to `(value: string[]) => void`.
    *   Pass `multiple={true}` to the `ComboboxSelect` used for Assets.
    *   Update `handleRandomize` to pass the random asset as an array `[randomAsset]`.

3.  **Update Main Page** (`app/page.tsx`)
    *   Change the `asset` state initialization to an empty array: `useState<string[]>([])`.
    *   Update the `isInspireDisabled` validation logic to check `asset.length === 0`.
    *   Pass the updated state and setter to `InspireTab`.

This will allow users to select multiple resources while keeping Track and Role as single selections.
